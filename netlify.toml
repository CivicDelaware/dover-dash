# ── Netlify Configuration — Dover Dash / Civic Delaware ──────────────────────

[build]
  publish   = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# ── Security headers ─────────────────────────────────────────────────────────
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options           = "SAMEORIGIN"
    X-Content-Type-Options    = "nosniff"
    Referrer-Policy           = "strict-origin-when-cross-origin"
    Permissions-Policy        = "camera=(), microphone=(), geolocation=()"

# ── Cache static assets aggressively ─────────────────────────────────────────
[[headers]]
  for = "*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "*.json"
  [headers.values]
    Cache-Control = "public, max-age=60"

# ── Branch deploys ────────────────────────────────────────────────────────────
# tracker-preview branch → preview URL only, not production
[context.tracker-preview]
  command = "echo 'Preview build'"

# Serve tracker.html at the root on the preview branch so /tracker.html
# doesn't need to be typed manually. Status 200 = transparent rewrite,
# not a redirect — URL stays at / in the browser.
[[context.tracker-preview.redirects]]
  from   = "/"
  to     = "/tracker.html"
  status = 200

[context.production]
  command = "echo 'Production build'"
